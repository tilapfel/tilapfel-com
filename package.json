{
  "name": "tilapfel-com",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Static site for tilapfel.com. No build step for deployment — these are dev-only tools for formatting and linting the source before commit.",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^9.19.0",
    "eslint": "^9.19.0",
    "globals": "^15.14.0",
    "prettier": "^3.4.2"
  }
}
