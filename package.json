{
  "name": "tilapfel-com",
  "version": "1.4.0",
  "private": true,
  "type": "module",
  "description": "Static site for tilapfel.com. No build step for deploying the site itself — Netlify Functions under netlify/functions/ provide the spam-check backend for the contact forms.",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint ."
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.19.0",
    "eslint": "^9.19.0",
    "globals": "^17.7.0",
    "prettier": "^3.4.2"
  }
}
